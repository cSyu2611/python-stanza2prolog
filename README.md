# python-stanza2prolog

## Setup

```
$ pip install stanza
$ python stanza2prolog_test.py
```

stanza2prolog_test.py ファイル内の`stanza.download`部は初回のみ走らせてください。

実行後、test_prolog.pl ファイル内で prolog の述語一覧を確認できます。
